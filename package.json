{
  "name": "pi-tk-workflow",
  "version": "0.1.0",
  "description": "Pi ticket implementation workflow: implement → review → fix → close.",
  "private": true,
  "keywords": ["pi-package", "pi-workflow"],
  "bin": {
    "irfc": "bin/irfc"
  },
  "scripts": {
    "setup": "./bin/irfc setup",
    "sync": "./bin/irfc sync"
  },
  "pi": {
    "prompts": ["prompts"]
  }
}
