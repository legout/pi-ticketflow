# Success Metrics

- Fix-step runs use the `fixer` meta-model when configured.
- No regressions: existing configs without `metaModels.fixer` still work.
- Users can change fixer behavior by editing settings only (no code change).
- Optional: reduced average cost/latency for fix iterations when using a cheaper fixer model.
