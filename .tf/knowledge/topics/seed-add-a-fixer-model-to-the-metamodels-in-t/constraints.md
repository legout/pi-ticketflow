# Constraints

- Must remain compatible with existing `.tf/config/settings.json` files.
- Must not break `/tf` commands that rely on the meta-model resolution pattern.
- Any new settings must be optional and have a safe default behavior.
