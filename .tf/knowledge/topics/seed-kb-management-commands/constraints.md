# Constraints: seed-kb-management-commands

- Implement in Python (`tf_cli/`); do not use `scripts/tf_legacy.sh`.
- Keep operations deterministic (no LLM required).
- Prefer stdlib only.
- Permanent deletion requested for `tf kb delete`.
