# Success Metrics: seed-kb-management-commands

- Users can enumerate and inspect topics without manually browsing directories.
- Archiving/restoring topics keeps `index.json` consistent.
- `validate` catches broken index entries and orphan folders.
- `rebuild-index` provides a reliable recovery path.
