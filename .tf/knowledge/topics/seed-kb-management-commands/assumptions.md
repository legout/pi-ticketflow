# Assumptions: seed-kb-management-commands

- The knowledge base directory is local and writable.
- Topic directories follow the documented structure.
- `index.json` is the canonical registry for active topics.
- Python CLI is available (tf_cli) and should be used instead of legacy shell tooling.
